{
    "SeizeDeliveryJobsEndpointFilter${EndpointId}": {
        "desc": "${Desc}",
        "enabled": false,
        "next": [
            "SeizeDeliveryJobsEndpointMatched"
        ],
        "focus": {
            "Node.Recognition.Succeeded": "$task.SeizeDeliveryJobs.focus.endpoint${EndpointId}"
        }
    }
}
